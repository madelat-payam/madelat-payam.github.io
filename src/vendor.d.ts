// Fontsource ships CSS-only variable packages without type declarations for the
// bare side-effect import, so the editor flags it. The import is real and Vite
// resolves it; this just tells the type checker the module exists.
declare module '@fontsource-variable/space-grotesk';
